export interface AdminUser { id:string; email:string; displayName:string; role:string; xp:number; createdAt:string; }
export interface AdminCourse { id:string; title:string; level:string; totalLessons:number; isPublished:boolean; }
